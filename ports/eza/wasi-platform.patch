diff --git a/src/fs/file.rs b/src/fs/file.rs
index 8e933ac..4ee09d8 100644
--- a/src/fs/file.rs
+++ b/src/fs/file.rs
@@ -1020,6 +1020,47 @@ impl<'dir> File<'dir> {
     }
 }
 
+#[cfg(target_os = "wasi")]
+impl<'dir> File<'dir> {
+    pub fn size(&self) -> f::Size {
+        if self.is_directory() {
+            f::Size::None
+        } else {
+            f::Size::Some(self.metadata().map_or(0, std::fs::Metadata::len))
+        }
+    }
+
+    fn recursive_directory_size(&self) -> RecursiveSize {
+        RecursiveSize::None
+    }
+
+    pub fn is_empty_dir(&self) -> bool {
+        self.is_directory() && self.is_empty_directory()
+    }
+
+    pub fn changed_time(&self) -> Option<NaiveDateTime> {
+        self.modified_time()
+    }
+
+    pub fn type_char(&self) -> f::Type {
+        if self.is_file() {
+            f::Type::File
+        } else if self.is_directory() {
+            f::Type::Directory
+        } else if self.is_link() {
+            f::Type::Link
+        } else {
+            f::Type::Special
+        }
+    }
+
+    pub fn security_context(&self) -> f::SecurityContext<'_> {
+        f::SecurityContext {
+            context: SecurityContextType::None,
+        }
+    }
+}
+
 impl<'a> AsRef<File<'a>> for File<'a> {
     fn as_ref(&self) -> &File<'a> {
         self
diff --git a/src/output/file_name.rs b/src/output/file_name.rs
index 99f9f15..15d11cf 100644
--- a/src/output/file_name.rs
+++ b/src/output/file_name.rs
@@ -473,6 +473,17 @@ impl<C: Colours> FileName<'_, '_, C> {
         }
     }
 
+    #[cfg(target_os = "wasi")]
+    pub(crate) fn classify_char(&self, file: &File<'_>) -> Option<&'static str> {
+        if file.is_directory() {
+            Some("/")
+        } else if file.is_link() {
+            Some("@")
+        } else {
+            None
+        }
+    }
+
     /// Returns at least one ANSI-highlighted string representing this file’s
     /// name using the given set of colours.
     ///
diff --git a/src/output/table.rs b/src/output/table.rs
index 741e75a..ac78b54 100644
--- a/src/output/table.rs
+++ b/src/output/table.rs
@@ -226,6 +226,14 @@ impl Column {
         }
     }
 
+    #[cfg(target_os = "wasi")]
+    pub fn alignment(self) -> Alignment {
+        match self {
+            Self::FileSize | Self::GitStatus | Self::Loc(_) => Alignment::Right,
+            _ => Alignment::Left,
+        }
+    }
+
     /// Get the text that should be printed at the top, when the user elects
     /// to have a header row printed.
     #[must_use]
@@ -236,6 +244,8 @@ impl Column {
             Self::Permissions => "Permissions",
             #[cfg(windows)]
             Self::Permissions => "Mode",
+            #[cfg(target_os = "wasi")]
+            Self::Permissions => "Mode",
             Self::FileSize => "Size",
             Self::Language => "Language",
             Self::Loc(CodeContent::Percent) => "Code %",
@@ -544,6 +554,14 @@ impl<'a> Table<'a> {
         })
     }
 
+    #[cfg(target_os = "wasi")]
+    fn permissions_plus(&self, file: &File<'_>, xattrs: bool) -> Option<f::PermissionsPlus> {
+        Some(f::PermissionsPlus {
+            file_type: file.type_char(),
+            xattrs,
+        })
+    }
+
     #[cfg(unix)]
     fn octal_permissions(&self, file: &File<'_>) -> Option<f::OctalPermissions> {
         file.permissions()
diff --git a/src/output/render/mod.rs b/src/output/render/mod.rs
index f6d2863..0726e80 100644
--- a/src/output/render/mod.rs
+++ b/src/output/render/mod.rs
@@ -34,4 +34,6 @@ mod permissions_unix;
 mod permissions_windows;
+#[cfg(target_os = "wasi")]
+mod permissions_wasi;
 
 mod size;
 pub use self::size::Colours as SizeColours;
diff --git a/src/output/render/permissions_wasi.rs b/src/output/render/permissions_wasi.rs
new file mode 100644
index 0000000..2f0fe99
--- /dev/null
+++ b/src/output/render/permissions_wasi.rs
@@ -0,0 +1,29 @@
+// SPDX-FileCopyrightText: 2026 EdgeTerm contributors
+// SPDX-License-Identifier: EUPL-1.2
+
+use crate::fs::fields as f;
+use crate::output::cell::{DisplayWidth, TextCell};
+use crate::output::render::FiletypeColours;
+
+use super::{PermissionsColours as Colours, PermissionsPlusRender};
+
+impl PermissionsPlusRender for Option<f::PermissionsPlus> {
+    fn render<C: Colours + FiletypeColours>(&self, colours: &C) -> TextCell {
+        match self {
+            Some(value) => {
+                let mut chars = vec![value.file_type.render(colours)];
+                if value.xattrs {
+                    chars.push(colours.attribute().paint("@"));
+                }
+                TextCell {
+                    width: DisplayWidth::from(chars.len()),
+                    contents: chars.into(),
+                }
+            }
+            None => TextCell {
+                width: DisplayWidth::from(0),
+                contents: vec![].into(),
+            },
+        }
+    }
+}
